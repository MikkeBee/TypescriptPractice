let aCharacter = "luigi";
let newAge = 30;
let isABlackBelt = false;

aCharacter = "mario";
newAge = 40;
isABlackBelt = true;

const cirkEl = (diameter: number) => {
  return diameter * Math.PI;
};

console.log(cirkEl(2));

console.log(character);

const formInputs = document.querySelectorAll("input");

console.log(formInputs);

formInputs.forEach((input) => {
  console.log(input);
});
